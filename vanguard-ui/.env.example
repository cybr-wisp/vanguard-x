# CARTO basemap key (required for the CARTO raster basemap)
VITE_CARTO_API_KEY=

# Optional. Defaults to the current browser hostname on port 8081.
# Example: localhost:8081
VITE_BACKEND_HOST=localhost:8081