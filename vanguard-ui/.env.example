# Vanguard UI does not require a client-side basemap API key.
# The tactical map uses Esri World Imagery and the
# World Boundaries and Places reference layer.

# Optional. Defaults to the current browser hostname on port 8081.
VITE_BACKEND_HOST=localhost:8081