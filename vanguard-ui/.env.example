# Vanguard UI uses same-origin backend routing.
#
# Production:
#   nginx proxies /api/* and /ws/* to vanguard-api:8081.
#
# Local development:
#   Vite proxies /api/* and /ws/* to localhost:8081.
#
# No client-side backend hostname or basemap API key is required.
